apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-demo
  namespace: python-gitops
  labels:
    app.kubernetes.io/name: gitopsdemo
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: gitopsdemo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gitopsdemo
    spec:
      serviceAccountName: gitopsdemo-sa
      containers:
        - name: gitopsdemo
          image: conyeukwu25/gitops-demo:5235bbc
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
